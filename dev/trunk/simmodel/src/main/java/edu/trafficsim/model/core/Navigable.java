package edu.trafficsim.model.core;

import edu.trafficsim.model.DataContainer;

public interface Navigable<L, N> extends DataContainer {
	
	// Ns next(L)
	// Ls next(N)
	// Ns previous(L)
	// Ls previous(N)

}
